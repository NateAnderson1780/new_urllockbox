<h1> Edit Your Link </h1>

<%= form_for(@link) do |l| %>
  <%= l.label :url %>
  <%= l.text_field :url %>
  <%= l.label :title %>
  <%= l.text_field :title %>
  
  <%= l.submit "Update Link" %>
<% end %>